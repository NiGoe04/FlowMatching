empirical observations:
- Bounded sets seem to work slightly better
- TO DO: Do all distributions work?
- If the variance is too huge, too tiny -> vector field collapses
    - Hypothesis: "The distributions match well when their support covers the same region with similar scale."
    -> This idea is intuitive for 2D point distributions, but how is it for e.g. pictures (MNIST!), calculate per pixel standard variance?




random notes:
- bounded gauss model does not benefit as much from more epochs during training

RES:
- experiment linspace an ts samplen im Training
- experiment t batch samplen PRO epoch
- experiment loss dxt = xt - x0

Herr Ochs hätte gerne:
- prior gauss + endverteilung nur ein Punkt
- vektorfeld nicht parametrisch in t

Roadmap theoretical FM
- von flow modeling choices bis hin zum training / implementation
- interaktion flow vs prob path vs field
    - inkl. marginal vs conditional path
- frage wahl vs was muss man (natur vom flowmatching) inkludieren
- siehe blog flow matching (ICML flowmatching)

Nächste Termine:
- paul nächste woche mittwoch
- alle in 2 wochen mittwoh