PARAMS = {
    "num_epochs": 10,         # leave as is
    "batch_size": 256,        # leave as is
    "learning_rate": 1e-2,    # leave as is
    "size_train_set": 200000, # leave as is
    "amount_samples": 1000,
    "solver_steps": 100,
    "num_times_to_visualize": 200,
    "t_end": 1.0,
    "field_density": 5,
    "field_bound": [-3, 3, -3, 3],
    "solver_method": 'midpoint'
}

noise_bound_target = 0.1